<% if @products.any? %>
  <% @products.each do |product| %>
    <%= product.name %>
    <%= button_to 'Add to Basket', items_path(product_id: product) %>
  <% end %>
<% else %>
  No products in our store yet.
<% end %>